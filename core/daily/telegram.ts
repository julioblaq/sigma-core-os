import type { SigmaDailyIssue } from './issues.js';
export async function notifySigmaDailyDraft(issue: SigmaDailyIssue) { const token = process.env.SIGMA_DAILY_TELEGRAM_BOT_TOKEN; const chatId = process.env.SIGMA_DAILY_TELEGRAM_CHAT_ID; if (!token || !chatId) return { delivered: false, skipped: 'not_configured' }; try { const response = await fetch(`https://api.telegram.org/bot${token}/sendMessage`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ chat_id: chatId, text: `<b>Sigma Daily draft ready for review</b>\n${issue.subject}\n\nUse /daily to read it and /dailyfeedback to suggest edits.`, parse_mode: 'HTML' }) }); return { delivered: response.ok }; } catch { return { delivered: false }; } }
