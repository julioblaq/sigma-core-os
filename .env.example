# Sigma Core OS — environment variables
# Copy to .env and fill in values

# Port for the API server (default: 3001)
PORT=3001

# Path to the SQLite database file (default: ./sigma.db)
DB_PATH=./sigma.db
